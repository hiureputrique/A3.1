import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:qualyfine/app/modules/categorias/categorias_page.dart';

main() {
  testWidgets('CategoriasPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CategoriasPage(title: 'Categorias')));
    //  final titleFinder = find.text('Categorias');
    //  expect(titleFinder, findsOneWidget);
  });
}
